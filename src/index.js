import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import Game from './Game';

ReactDOM.render(
  // Strict Mode: https://reactjs.org/docs/strict-mode.html
  // use React Strict Mode if required
    <Game />,
  document.getElementById('root')
);
